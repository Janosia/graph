{
  "name": "tool-dependency-graph-generator",
  "version": "1.0.0",
  "description": "Generates a tool dependency graph (dependency_graph.json) from a Composio-style toolkit catalog.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "true",
    "generate": "node --import tsx src/generate.ts",
    "selfcheck": "node --import tsx src/generate.ts github_catalog.json && node scripts/build_visualization.js",
    "build-catalog": "node scripts/build_catalog.js",
    "visualize": "node scripts/build_visualization.js"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}